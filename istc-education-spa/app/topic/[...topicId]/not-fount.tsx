import NotFound from "@/components/html/not-found";

const TopicsDetailNotFound: React.FC = () => {
    return (
        <NotFound>
            <p>Sorry, the topic you are looking for does not exist.</p>
        </NotFound>
    );
}

export default TopicsDetailNotFound;